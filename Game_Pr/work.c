#include "work.h"

void perfectpixel(/*SDL_Surface *backgroundG,SDL_Surface *backgroundGMasque,*/SDL_Surface *screen,/*SDL_Rect screen_rect,*/int gameW)
{
  /*SDL_Rect scrn_rect;
	scrn_rect.x=0;
	scrn_rect.y=0;

  //-------------------Backgroundgame1

  SDL_Surface *backgroundG=NULL;
  backgroundG=IMG_Load("BackgroundG.png");

  // Masuqe

  SDL_Surface *backgroundGMasque=NULL;
  backgroundGMasque=SDL_LoadBMP("BackgroundGMasque.bmp");

  SDL_BlitSurface(backgroundGMasque,NULL,screen,&scrn_rect);
  SDL_BlitSurface(backgroundG,NULL,screen,&scrn_rect);

  SDL_Event event;
  while (SDL_PollEvent(&event))
  {
    switch(event.type)
    {
      case SDL_QUIT:
        gameW=0;
      break;
    }
    SDL_Flip(screen);
  }*/
}
